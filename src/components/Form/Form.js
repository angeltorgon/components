/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function Form(props) {
  return (
    <form>{props.children}</form>
  )
};

/**
 * Export component
 */

export default Form;
