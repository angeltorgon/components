/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function Text(props) {
  return (
    <span>{props.children}</span>
  )
};

/**
 * Export component
 */

export default Text;
