/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function Section(props) {
  return (
    <section>{props.children}</section>
  )
};

/**
 * Export component
 */

export default Section;
