/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function TableBody(props) {
  return (
    <tbody>{props.children}</tbody>
  )
};

/**
 * Export component
 */

export default TableBody;
