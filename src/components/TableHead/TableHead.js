/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function TableHead(props) {
  return (
    <thead>{props.children}</thead>
  )
};

/**
 * Export component
 */

export default TableHead;
