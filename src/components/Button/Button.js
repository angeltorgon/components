/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function Button(props) {
  return (
    <button>{props.children}</button>
  )
};

/**
 * Export component
 */

export default Button;
