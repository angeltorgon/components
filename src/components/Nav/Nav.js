/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function Nav(props) {
  return (
    <nav>{props.children}</nav>
  )
};

/**
 * Export component
 */

export default Nav;
