/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function Select(props) {
  return (
    <select>{props.children}</select>
  )
};

/**
 * Export component
 */

export default Select;
