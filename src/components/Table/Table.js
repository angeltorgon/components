/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function Table(props) {
  return (
    <table>{props.children}</table>
  )
};

/**
 * Export component
 */

export default Table;
