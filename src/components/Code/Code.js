/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function Code(props) {
  return (
    <code>{props.children}</code>
  )
};

/**
 * Export component
 */

export default Code;
