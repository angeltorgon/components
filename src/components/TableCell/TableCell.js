/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function TableCell(props) {
  return (
    <td>{props.children}</td>
  )
};

/**
 * Export component
 */

export default TableCell;
