/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function Header(props) {
  return (
    <header>{props.children}</header>
  )
};

/**
 * Export component
 */

export default Header;
