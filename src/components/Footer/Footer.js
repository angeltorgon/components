/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function Footer(props) {
  return (
    <footer>{props.children}</footer>
  )
};

/**
 * Export component
 */

export default Footer;
