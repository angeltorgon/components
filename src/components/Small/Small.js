/**
 * Dependencies
 */

import React from 'react';

/**
 * Define component
 */

function Small(props) {
  return (
    <small>{props.children}</small>
  )
};

/**
 * Export component
 */

export default Small;
